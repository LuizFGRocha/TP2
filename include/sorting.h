#include "vetor.h"

/// @todo
template <typename T>
vetor<T> mergesort(vetor<T> vetor_entrada);

template <typename T>
vetor<T> insertionsort(vetor<T> vetor_entrada);

template<typename T>
vetor<T> linearsort(vetor<T> vetor_entrada);